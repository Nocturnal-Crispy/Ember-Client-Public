{
  "name": "ember-client",
  "version": "1.0.0",
  "description": "Ember Client",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron . --no-sandbox",
    "dev": "npm run build && electron . --no-sandbox"
  },
  "keywords": ["typescript", "electron", "gui"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "electron": "^28.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
