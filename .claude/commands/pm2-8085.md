Start ember-server (ports 8085/8086) and follow logs.
```bash
cd /home/mcrispen/projects/ember && ~/.local/share/npm/bin/pm2 start ecosystem.config.cjs --only ember-server-8085 && ~/.local/share/npm/bin/pm2 logs ember-server-8085
```
