Start ember-client (Electron) and follow logs.
```bash
cd /home/mcrispen/projects/ember && ~/.local/share/npm/bin/pm2 start ecosystem.config.cjs --only ember-client && ~/.local/share/npm/bin/pm2 logs ember-client
```
